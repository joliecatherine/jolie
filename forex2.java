public class forex2 { // Define a public class named "FOR2"
    public static void main(String[] args) { // Define the main method, the entry point of the program
        for(int i=30; i<=31; i++){ // Start a "for" loop with an initialization of 'i' to 30,
        // a condition 'i <= 31', and an increment 'i++' to iterate through values from 10 to 12 (inclusive).
            System.out.println("Dates before this:" +i); //Within the loop, print a message indicating the dates before this,
            // with the value of 'i' included in the message.
        } // End of the "for" loop
    }
    
}